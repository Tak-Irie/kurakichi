describe('パスワード変更テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:SSOユーザーはパスワード変更不可', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:古いパスワードが一致しない', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:新しいパスワードが用件を満たしていない', () => {
    expect(some).toBeTruthy();
  });
});
