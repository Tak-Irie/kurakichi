describe('団体参加申請テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:団体が存在していない', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:ユーザーのIDが不正な値である', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:団体のIDが不正な値である', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:既に参加申請を行っている', () => {
    expect(some).toBeTruthy();
  });
});
