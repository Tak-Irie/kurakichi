describe('団体による問い合わせ総取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:問い合わせが存在していません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:団体が存在していません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:不正なID形式です', () => {
    expect(some).toBeTruthy();
  });
});
