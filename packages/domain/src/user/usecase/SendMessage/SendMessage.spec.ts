describe('メッセージ送信テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:送信先が不明', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:入力内容が不適当', () => {
    expect(some).toBeTruthy();
  });
});
