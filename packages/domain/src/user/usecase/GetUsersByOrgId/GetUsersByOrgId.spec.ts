describe('団体所属ユーザー取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:団体が存在していません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:ユーザーが存在していません', () => {
    expect(some).toBeTruthy();
  });
});
