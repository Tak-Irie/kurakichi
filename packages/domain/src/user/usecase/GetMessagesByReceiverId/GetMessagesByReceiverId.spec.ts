describe('ユーザー固有のメッセージ総取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:データも空配列も帰ってこない', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:入力値が不適当', () => {
    expect(some).toBeTruthy();
  });
});
