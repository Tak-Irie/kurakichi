describe('ステータス指定お問い合わせ取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:該当するお問い合わせが存在しない', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:入力された値が不適当', () => {
    expect(some).toBeTruthy();
  });
});
