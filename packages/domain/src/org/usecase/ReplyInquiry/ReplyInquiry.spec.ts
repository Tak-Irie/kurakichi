describe('お問い合わせ返信テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:返信先が見つかりません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:返信権限がありません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:団体メンバーではありません', () => {
    expect(some).toBeTruthy();
  });
});
