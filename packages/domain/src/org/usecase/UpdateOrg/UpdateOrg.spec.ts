describe('団体情報更新テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:入力内容が不適当', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:更新権限がない', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:更新対象が存在しない', () => {
    expect(some).toBeTruthy();
  });
});
