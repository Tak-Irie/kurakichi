describe('団体メンバー登録テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:ユーザーが既にメンバーに登録済み', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:ユーザーが存在しない', () => {
    expect(some).toBeTruthy();
  });
  test('申請を許可する権限がない', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:入力された値が不正', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:組織が存在しない', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:ユーザーが申請を取り下げている', () => {
    expect(some).toBeTruthy();
  });
});
