describe('メッセージ取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:メッセージが存在しない', () => {
    expect(some).toBeTruthy();
  });

  test('失敗:ログインしていない', () => {
    expect(some).toBeTruthy();
  });
});
