describe('お問い合わせ状況更新テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:NotExist', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:Statusの値が不正', () => {
    expect(some).toBeTruthy();
  });
});
