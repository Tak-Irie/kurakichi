describe('ユーザー取得テスト', () => {
  const some = undefined;
  test('成功', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:ユーザーが存在しません', () => {
    expect(some).toBeTruthy();
  });
  test('失敗:IDの値が不正です', () => {
    expect(some).toBeTruthy();
  });
});
