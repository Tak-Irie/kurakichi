mutation UpdateUser($email: String, $description: String, $avatar: String, $image: String) {
  updateUser(email: $email, description: $description, avatar: $avatar, image: $image) {
    user {
      ...UserPayload
    }
    error {
      ...RegularError
    }
  }
}
